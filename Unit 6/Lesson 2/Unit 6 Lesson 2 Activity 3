public class U6_L2_Activity_Three {
    // Write your numDivisible method here
    public static int numDivisible(int num, int[] vals) {
        int z = 0;
        for (int y: vals) {
            if (y % num == 0)
                z++;
        }
        return z;
    }
}

// Passed